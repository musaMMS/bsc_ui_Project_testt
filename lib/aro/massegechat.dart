import 'package:flutter/material.dart';

class massegeChat extends StatelessWidget {
  const massegeChat({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
